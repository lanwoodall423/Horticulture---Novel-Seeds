# Horticulture Dev Bridge Workflow

Use the owner descriptor and validator before testing. Discover a live bridge and query `lan.horticulture.novelseeds` context; do not infer generations or fingerprints from source files. Publish and reload only a validated adapter-only change. Request a full restart for gameplay or core changes, then query context again and discard old authority and cached handles.
