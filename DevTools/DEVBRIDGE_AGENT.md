# Horticulture Dev Bridge Workflow

Use the owner descriptor and validator before testing. Discover a live bridge and query `lan.horticulture.novelseeds` context; do not infer generations or fingerprints from source files. Publish and reload only a validated adapter-only change. For a coordinator-owned sandbox request `restart request --agent-id <id> --package-id lan.horticulture.novelseeds --readiness game --save-policy none`; attached/live processes require a person or external orchestrator. Then query context again and discard old authority and cached handles.
